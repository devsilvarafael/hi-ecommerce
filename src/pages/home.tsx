import { Box } from "@chakra-ui/react";

const Home = () => (
  <Box
    display="flex"
    w="100%"
    flexDirection="column"
    alignItems="center"
    mt="6rem"
  >
    <p>Home</p>
  </Box>
);

export default Home;
